apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-umbrella-test.fullname" . }}
  labels:
    {{- include "helm-umbrella-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-umbrella-test.selectorLabels" . | nindent 4 }}
